{
  "name": "smart-inventory",
  "version": "1.0.0",
  "description": "Welcome to your new CAP project.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch-inventory-ui": "cds watch --open inventoryui/index.html?sap-ui-xx-viewCache=false --livereload false"
  },
  "dependencies": {
    "@sap/cds": "^9.9.1",
    "@sap/xssec": "^4.13.0",
    "passport": "^0.7.0",
    "@cap-js/sqlite": "^3.0.1"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": {
          "database": "db.sqlite"
        }
      },
      "auth": "mocked"
    }
  },
  "workspaces": [
    "app/*"
  ],
  "devDependencies": {
    "cds-plugin-ui5": "^0.17.0"
  },
  "sapux": [
    "app/inventory-ui"
  ]
}
